				<group>
					<isLeft> [[WHEEL_IS_LEFT]] </isLeft>
					<template> [[WHEEL_NAME_OR_TEMPLATE]] </template>
					<radius> [[WHEEL_RADIUS]] </radius>
					<startIndex> [[WHEEL_GROUP_STARTINDEX]] </startIndex>
					<count> [[WHEEL_GROUP_COUNT]] </count>
				</group>