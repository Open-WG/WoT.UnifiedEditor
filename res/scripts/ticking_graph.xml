<root>
    <Init>
        <preInitGroup> <!-- this group is called just before Init -->
            <before> true </before>
        </preInitGroup>
    </Init>
    <Simulation>
        <PreSimulation>
            <before> true </before>
        </PreSimulation>

        <NetworkFilter>
            <before> true </before>
        </NetworkFilter>

        <Sensor/>
    </Simulation>
    <PostTick>
        <postTickUpdate/>
        <postTickUpdateFinish/>
    </PostTick>
    <Finalize>
        <preFinalize>
            <before> true </before>
        </preFinalize>
    </Finalize>
</root>